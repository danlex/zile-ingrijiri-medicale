# This domain is a permanent redirect to varzaru.ro.
# index.html and 404.html are plain-HTML redirect stubs (no front matter),
# so Jekyll copies them verbatim and every URL on this domain redirects
# to the matching page on varzaru.ro.
title: "Redirect to varzaru.ro"
url: "https://zile-ingrijiri-medicale.ro"
baseurl: ""

exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
  - vendor/
